export const DEFAULT_COLUMNS_COUNT = 1

export const DEFAULT_GUTTER = 20

export const DEFAULT_COLUMNS_COUNT_POINTS = { 1000: 4 }

export enum MasonryType {
  'column' = 'column',
  'flex' = 'flex',
}

export enum MasonryDirection {
  'row' = 'row',
  'column' = 'column',
}
